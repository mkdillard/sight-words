import React from 'react';
import './SightWords.css';

const SightWords = ({type}) => {
  return (
      <div className='sight-words'>
        Hola from SightWords type {type}
      </div>
  );
}

export default SightWords;
